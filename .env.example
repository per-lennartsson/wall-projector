# Copy to .env (repo root) and fill in real values before running
# docker-compose.web.yml. Never commit the real .env.
POSTGRES_PASSWORD=
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
